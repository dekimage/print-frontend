// const [GET_STATE_QUERY, setGET_STATE_QUERY] = useState(GET_UNIFORGE);
// const games = ["uniforges", "uniheros"];
// const [selectedGame, setSelectedGame] = useState("uniforges");

// function changeGame(event, value) {
//   console.log(event);
//   setSelectedGame(value);
//   if (value === "uniforges") {
//     setGET_STATE_QUERY(GET_UNIFORGE);
//     getGame();
//   }
//   if (value === "uniheroes") {
//     setGET_STATE_QUERY(GET_UNIHERO);
//     getGame();
//   }
// }

{
  /* <Autocomplete
disablePortal
onChange={(e) => changeGame(e)}
onKeyPress={(e) => handleKeyDown(e, "game")}
id="combo-box-demo"
options={games}
sx={{ width: 150 }}
renderInput={(params) => (
  <TextField {...params} label="Search Card..." />
)}
/>
<Button
style={{ margin: "1rem" }}
variant="contained"
onClick={() => loadGame()}
>
LOAD GAME
</Button> */
}
